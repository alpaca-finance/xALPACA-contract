[profile.default]
src = 'contracts'
out = 'out'
libs = ["node_modules"]
test = 'tests/forge'
fs_permissions = [{ access = "read-write", path = "./"}]
cache_path = './forge_cache'
optimizer = true
optimizer-runs = 200
